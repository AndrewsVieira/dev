/**
Com ponteiros, crie uma estrutura com suporte para 5 nomes de 10 posições.
Popule a matriz e imprima. Aumente a estrutura (sem perder os que já estão
nela) para 15 nomes e popule os faltantes. Imprima a estrutura. [fixacao9.c]
*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*
int main()
{
    char *nomes;

    nomes = (char *)malloc(55 * sizeof(int));

    strcpy(*nomes, "Andrews");
    printf("%d", *nomes);
    nomes += 11;
    strcpy(*nomes, "João");
    printf("%d", *nomes);
    nomes += 11;
    strcpy(*nomes, "Marco");
    printf("%d", *nomes);
    nomes += 11;
    strcpy(*nomes, "Marília");
    printf("%d", *nomes);
    nomes += 11;
    strcpy(*nomes, "Marcia");
    printf("%d", *nomes);
    nomes += 11;
    nomes -= 55;

    free(nomes);
}
*/